[phases.setup]
nixPkgs = ["nodejs_20", "python3", "gcc", "gnumake"]

[phases.install]
cmds = ["npm install --omit=dev --ignore-scripts", "npm rebuild better-sqlite3"]

[phases.build]
cmds = ["cd ui && npm install && npm run build && cd .."]

[start]
cmd = "node scripts/start.js"
