# ACC v2 — Current Task for Cursor

## Status: READY
## Assigned: Cursor
## Reviewed by: Claude

---

## Task: Scaffold 10 Integration Connectors

Create the following files. Each must:
- Export `checkHealth()` and `enabled()` 
- Log warning if API key missing
- Fail gracefully — never crash the server

### Files to create:

1. `cloud/integrations/langfuse.js` — Langfuse tracing wrapper
2. `cloud/integrations/openrouter.js` — OpenRouter AI gateway
3. `cloud/integrations/qdrant.js` — Qdrant vector memory
4. `cloud/integrations/sentry.js` — Sentry error tracking
5. `cloud/integrations/helicone.js` — Helicone AI analytics
6. `cloud/integrations/n8n.js` — n8n webhook bridge
7. `cloud/integrations/supabase.js` — Supabase auth/db adapter
8. `cloud/integrations/browserbase.js` — Browserbase cloud browser
9. `cloud/integrations/flowise.js` — Flowise experimental bridge
10. `cloud/integrations/neo4j.js` — Neo4j graph connector

### Status endpoint to update:
`GET /api/system/integrations/status` — return all 10 statuses

### Rules:
- CommonJS (require/module.exports)
- Read keys from process.env only
- Each file max ~50 lines
- console.warn if key missing, never throw
- See `.cursor/rules` for full architecture context

### Env vars needed (add to .env.example):
```
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
OPENROUTER_API_KEY=
QDRANT_URL=
QDRANT_API_KEY=
SENTRY_DSN=
HELICONE_API_KEY=
N8N_WEBHOOK_URL=
SUPABASE_SERVICE_ROLE_KEY=
BROWSERBASE_API_KEY=
BROWSERBASE_PROJECT_ID=
FLOWISE_API_URL=
FLOWISE_API_KEY=
NEO4J_URI=
NEO4J_USER=
NEO4J_PASSWORD=
```

---
Generated by Claude Orchestrator | ACC Task Bus
