on
{
  "summary": "Complete UI spec for ACC v2 dark dashboard redesign with React + Vite + Tailwind CSS, including color system, component specs, and router structure.",
  "output": "# ACC v2 Dashboard - Complete UI Specification\n\n## 1. Color System & Design Tokens\n\n```css\n/* tailwind.config.js additions */\ncolors: {\n  primary: {\n    50: '#EEF2FF',\n    100: '#E0E7FF',\n    200: '#C7D2FE',\n    300: '#A5B4FC',\n    400: '#818CF8',\n    500: '#6366F1',\n    600: '#4F46E5',\n    700: '#4338CA',\n    800: '#3730A3',\n    900: '#312E81',\n  },\n  success: '#10B981',\n  warning: '#F59E0B',\n  danger: '#EF4444',\n  surface: {\n    DEFAULT: '#111118',\n    light: '#1A1A24',\n    lighter: '#22222E',\n  },\n  background: '#0A0A0F',\n  border: 'rgba(255,255,255,0.06)',\n}\n```\n\n## 2. Layout Structure\n\n### Sidebar Navigation\n- **Expanded**: 240px width, `w-60`\n- **Collapsed**: 64px width, `w-16`\n- **Transition**: `transition-all duration-300 ease-in-out`\n- **Background**: `bg-surface border-r border-border`\n\n```jsx\n// Sidebar component structure\n<div className={`fixed left-0 top-0 h-full ${isCollapsed ? 'w-16' : 'w-60'} bg-surface border-r border-border z-50 transition-all duration-300`}>\n  {/* Logo area */}\n  <div className=\"h-16 flex items-center px-4 border-b border-border\">\n    {!isCollapsed && <span className=\"text-white font-bold text-lg\">ACC v2</span>}\n    <button onClick={toggleSidebar} className=\"ml-auto text-gray-400 hover:text-white\">\n      <MenuIcon className=\"w-5 h-5\" />\n    </button>\n  </div>\n  \n  {/* Navigation items */}\n  <nav className=\"mt-4 space-y-1 px-2\">\n    {navItems.map(item => (\n      <NavLink\n        key={item.path}\n        to={item.path}\n        className={({ isActive }) =>\n          `flex items-center px-3 py-2.5 rounded-lg transition-colors ${\n            isActive\n              ? 'bg-primary/10 text-primary border border-primary/20'\n              : 'text-gray-400 hover:bg-surface-light hover:text-white'\n          }`\n        }\n      >\n        <item.icon className=\"w-5 h-5 flex-shrink-0\" />\n        {!isCollapsed && <span className=\"ml-3 text-sm\">{item.label}</span>}\n      </NavLink>\n    ))}\n  </nav>\n</div>\n```\n\n### Top Bar\n- **Height**: `h-16`\n- **Background**: `bg-surface/80 backdrop-blur-md border-b border-border`\n- **Position**: `fixed top-0 right-0 z-40`\n- **Width**: `left-60` (expanded) / `left-16` (collapsed)\n\n```jsx\n<div className={`fixed top-0 ${isCollapsed ? 'left-16' : 'left-60'} right-0 h-16 bg-surface/80 backdrop-blur-md border-b border-border flex items-center justify-between px-6 transition-all duration-300`}>\n  {/* Left: Page title */}\n  <h1 className=\"text-white text-lg font-semibold\">{pageTitle}</h1>\n  \n  {/* Right: Status indicators */}\n  <div className=\"flex items-center space-x-4\">\n    {/* System Status Pill */}\n    <div className=\"flex items-center px-3 py-1.5 bg-success/10 border border-success/20 rounded-full\">\n      <div className=\"w-2 h-2 bg-success rounded-full animate-pulse mr-2\" />\n      <span className=\"text-success text-xs font-medium\">All Systems Normal</span>\n    </div>\n    \n    {/* DeepSeek Active Indicator */}\n    <div className=\"flex items-center px-3 py-1.5 bg-primary/10 border border-primary/20 rounded-full\">\n      <SparklesIcon className=\"w-3.5 h-3.5 text-primary mr-1.5\" />\n      <span className=\"text-primary text-xs font-medium\">DeepSeek Active</span>\n    </div>\n    \n    {/* User Count */}\n    <div className=\"flex items-center text-gray-400 text-sm\">\n      <UsersIcon className=\"w-4 h-4 mr-1\" />\n      <span>24</span>\n    </div>\n    \n    {/* Settings */}\n    <button className=\"text-gray-400 hover:text-white transition-colors\">\n      <SettingsIcon className=\"w-5 h-5\" />\n    </button>\n  </div>\n</div>\n```\n\n## 3. Page Components\n\n### Dashboard Page (`/dashboard`)\n\n#### Stat Cards (4 cards grid)\n```jsx\n<div className=\"grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4 mb-6\">\n  {stats.map((stat, index) => (\n    <div key={index} className=\"bg-surface rounded-xl border border-border p-4 hover:border-primary/30 transition-all\">\n      <div className=\"flex items-center justify-between mb-2\">\n        <span className=\"text-gray-400 text-xs font-medium uppercase tracking-wider\">{stat.label}</span>\n        <stat.icon className=\"w-4 h-4 text-primary\" />\n      </div>\n      <div className=\"text-2xl font-bold text-white\">{stat.value}</div>\n      <div className=\"flex items-center mt-1\">\n        <span className={`text-xs ${stat.trend > 0 ? 'text-success' : 'text-danger'}`}>\n          {stat.trend > 0 ? '↑' : '↓'} {Math.abs(stat.trend)}%\n        </span>\n        <span className=\"text-gray-500 text-xs ml-1\">vs last week</span>\n      </div>\n    </div>\n  ))}\n</div>\n```\n\n#### Live Task Feed\n```jsx\n<div className=\"bg-surface rounded-xl border border-border p-4 mb-6\">\n  <div className=\"flex items-center justify-between mb-4\">\n    <h2 className=\"text-white font-semibold\">Live Task Feed</h2>\n    <div className=\"flex items-center\">\n      <div className=\"w-2 h-2 bg-success rounded-full animate-pulse mr-2\" />\n      <span className=\"text-gray-400 text-xs\">Live</span>\n    </div>\n  </div>\n  \n  <div className=\"space-y-2 max-h-96 overflow-y-auto\">\n    {tasks.map((task) => (\n      <div key={task.id} className=\"flex items-center justify-between p-3 bg-surface-light rounded-lg border border-border hover:border-primary/20 transition-all\">\n        <div className=\"flex items-center space-x-3\">\n          <div className={`w-2 h-2 rounded-full ${\n            task.status === 'running' ? 'bg-primary animate-pulse' :\n            task.status === 'completed' ? 'bg-success' :\n            task.status === 'failed' ? 'bg-danger' : 'bg-warning'\n          }`} />\n          <span className=\"text-gray-300 text-sm\">{task.description}</span>\n        </div>\n        <div className=\"flex items-center space-x-2\">\n          <span className=\"px-2 py-0.5 bg-surface-lighter rounded text-xs text-gray-400\">{task.provider}</span>\n          <span className={`px-2 py-0.5 rounded text-xs font-medium ${\n            task.status === 'completed' ? 'bg-success/10 text-success' :\n            task.status === 'running' ? 'bg