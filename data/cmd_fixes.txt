on
{
  "summary": "Fix /latesttask, /taskstats, and /agents commands in telegramCommands.js to show real data from the task store and integration configurations.",
  "output": "## Analysis\n\nThe three commands in `cloud/taskbus/telegramCommands.js` need to be rewritten to pull real data from the task store and integration modules instead of showing placeholder or truncated data.\n\n### 1. /latesttask\n- **Current issue**: Shows task detail but output is cut off or missing\n- **Fix**: Query `store.getTasks()` sorted by `updated_at` descending, find the first task with status 'done', then call `store.getLatestResult(task.id)` to get the full output. Display up to 3000 characters of the output.\n\n### 2. /taskstats\n- **Current issue**: Unknown behavior (likely placeholder)\n- **Fix**: Aggregate tasks from `store.getTasks()` to compute:\n  - `total`: total number of tasks\n  - `done`: count of tasks with status 'done'\n  - `failed`: count of tasks with status 'failed'\n  - `pending`: count of tasks with status 'pending' or 'running'\n  - `by_provider`: breakdown by provider (deepseek, smart_stub, etc.)\n  - `today_count`: tasks where `created_at` is today's date\n\n### 3. /agents\n- **Current issue**: Likely missing or placeholder\n- **Fix**: Check each integration file's `enabled()` function for the following agents: deepseek, openai, openhands, crewai, aider, alphonso, composio, tavily, meta, perplexity. For each, show:\n  - Agent name\n  - Enabled: yes/no (based on `enabled()`)\n  - Key set: yes/no (check if API key is configured)\n\n## Recommended Code Changes\n\nReplace the command handlers in `cloud/taskbus/telegramCommands.js` with the following:\n\n```javascript\n// /latesttask command handler\nbot.command('latesttask', async (ctx) => {\n  try {\n    const tasks = store.getTasks();\n    if (!tasks || tasks.length === 0) {\n      return ctx.reply('No tasks found.');\n    }\n    \n    // Sort by updated_at descending\n    const sortedTasks = tasks.sort((a, b) => new Date(b.updated_at) - new Date(a.updated_at));\n    \n    // Find first completed task\n    const latestDoneTask = sortedTasks.find(task => task.status === 'done');\n    if (!latestDoneTask) {\n      return ctx.reply('No completed tasks found.');\n    }\n    \n    // Get full output\n    const result = store.getLatestResult(latestDoneTask.id);\n    const output = result ? result.output || result.result || '' : '';\n    \n    // Truncate to 3000 chars\n    const truncatedOutput = output.length > 3000 ? output.substring(0, 3000) + '\\n\\n... (truncated)' : output;\n    \n    const message = `*Latest Completed Task*\\n\\n` +\n      `*ID*: ${latestDoneTask.id}\\n` +\n      `*Provider*: ${latestDoneTask.provider || 'unknown'}\\n` +\n      `*Created*: ${latestDoneTask.created_at || 'unknown'}\\n` +\n      `*Completed*: ${latestDoneTask.updated_at || 'unknown'}\\n` +\n      `*Prompt*: ${(latestDoneTask.prompt || '').substring(0, 200)}\\n\\n` +\n      `*Full Output*:\\n\\`\\`\\`\\n${truncatedOutput}\\n\\`\\`\\``;\n    \n    await ctx.replyWithMarkdown(message);\n  } catch (error) {\n    console.error('Error in /latesttask:', error);\n    await ctx.reply('Error retrieving latest task: ' + error.message);\n  }\n});\n\n// /taskstats command handler\nbot.command('taskstats', async (ctx) => {\n  try {\n    const tasks = store.getTasks();\n    if (!tasks || tasks.length === 0) {\n      return ctx.reply('No tasks found in the system.');\n    }\n    \n    const total = tasks.length;\n    const done = tasks.filter(t => t.status === 'done').length;\n    const failed = tasks.filter(t => t.status === 'failed').length;\n    const pending = tasks.filter(t => t.status === 'pending' || t.status === 'running').length;\n    \n    // By provider breakdown\n    const byProvider = {};\n    tasks.forEach(t => {\n      const provider = t.provider || 'unknown';\n      byProvider[provider] = (byProvider[provider] || 0) + 1;\n    });\n    \n    // Today's count\n    const today = new Date();\n    today.setHours(0, 0, 0, 0);\n    const todayCount = tasks.filter(t => {\n      const created = new Date(t.created_at);\n      return created >= today;\n    }).length;\n    \n    const providerSummary = Object.entries(byProvider)\n      .map(([provider, count]) => `  - ${provider}: ${count}`)\n      .join('\\n');\n    \n    const message = `*Task Dashboard*\\n\\n` +\n      `*Total*: ${total}\\n` +\n      `*Done*: ${done}\\n` +\n      `*Failed*: ${failed}\\n` +\n      `*Pending*: ${pending}\\n` +\n      `*Today*: ${todayCount}\\n\\n` +\n      `*By Provider*:\\n${providerSummary}`;\n    \n    await ctx.replyWithMarkdown(message);\n  } catch (error) {\n    console.error('Error in /taskstats:', error);\n    await ctx.reply('Error retrieving task stats: ' + error.message);\n  }\n});\n\n// /agents command handler\nbot.command('agents', async (ctx) => {\n  try {\n    const agentModules = [\n      { name: 'DeepSeek', file: './integrations/deepseek.js', keyEnv: 'DEEPSEEK_API_KEY' },\n      { name: 'OpenAI', file: './integrations/openai.js', keyEnv: 'OPENAI_API_KEY' },\n      { name: 'OpenHands', file: './integrations/openhands.js', keyEnv: 'OPENHANDS_API_KEY' },\n      { name: 'CrewAI', file: './integrations/crewai.js', keyEnv: 'CREWAI_API_KEY' },\n      { name: 'Aider', file: './integrations/aider.js', keyEnv: 'AIDER_API_KEY' },\n      { name: 'Alphonso', file: './integrations/alphonso.js', keyEnv: 'ALPHONSO_API_KEY' },\n      { name: 'Composio', file: './integrations/composio.js', keyEnv: 'COMPOSIO_API_KEY' },\n      { name: 'Tavily', file: './integrations/tavily.js', keyEnv: 'TAVILY_API_KEY' },\n      { name: 'Meta', file: './integrations/meta.js', keyEnv: 'META_API_KEY' },\n      { name: 'Perplexity', file: './integrations/perplexity.js', keyEnv: 'PERPLEXITY_API_KEY' }\n    ];\n    \n    const agentStatuses = [];\n    \n    for (const agent of agentModules) {\n      try {\n        const mod = require(agent.file);\n        const enabled = typeof mod.enabled === 'function' ? await mod.enabled() : false;\n        const keySet = process.env[agent.keyEnv] ? true : false;\n        agentStatuses.push(`*${agent.name}*: Enabled: ${enabled ? '✅' : '❌'}, Key: ${keySet ? '✅' : '❌'}`);\n      } catch (err) {\n        agentStatuses.push(`*${agent.name}*: Error loading module - ${err.message}`);\n      }\n    }\n    \n    const message = `*Configured Agents*\\n\\n${agentStatuses.join('\\n')}`;\n    await ctx.replyWithMarkdown(message);\n  } catch (error) {\n    console.error('Error in /agents:', error);\n    await ctx.reply('Error retrieving agent statuses: ' + error.message);\n  }\n});\n```\n\n## Recommendation\n\n1.