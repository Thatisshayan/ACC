```json
{
  "summary": "Designed and implemented complete voice recognition route for ACC v2 with Express backend and React frontend components.",
  "output": "## Analysis and Recommendation\n\n### Architecture Overview\n\nThe voice recognition system follows a client-server architecture:\n- **Frontend**: React component (`VoiceInput.jsx`) captures audio via MediaRecorder API and sends it to the backend\n- **Backend**: Express route (`voiceRoute.js`) processes audio using OpenAI Whisper API with DeepSeek fallback, strips wake words, and routes commands to the Task Bus\n\n### Key Design Decisions\n\n1. **Wake Word Handling**: Case-insensitive regex strips \"hey acc\", \"acc\", \"okay acc\" from the beginning of transcripts\n2. **Dual Transcription**: Primary uses OpenAI Whisper API; falls back to DeepSeek if Whisper is unavailable\n3. **Direct Text Input**: Allows sending text directly (useful for testing or non-audio scenarios)\n4. **Electron Compatibility**: Uses standard Web APIs (MediaRecorder, FormData) that work in both browser and Electron\n5. **Dark Theme**: Matches ACC's existing `bg-[#0a0a0f]` color scheme with Tailwind CSS\n\n### Files Changed\n- `cloud/connectors/voiceRoute.js` (new)\n- `ui/src/VoiceInput.jsx` (new)\n- `cloud/server.js` (modified - added route import)\n\n### Implementation Details\n\n**Backend (voiceRoute.js)**:\n- POST `/api/voice/transcribe` accepts multipart/form-data with `audio` field or JSON with `text` field\n- Uses `multer` for file upload handling\n- Transcribes audio via OpenAI Whisper API with error fallback to DeepSeek\n- Strips wake words using regex: `/^(hey\\s+)?(okay\\s+)?acc[,\\s]+/i`\n- Routes cleaned command to Task Bus at `/api/taskbus/task`\n- Returns `{ success, transcript, taskId, routing }`\n\n**Frontend (VoiceInput.jsx)**:\n- Microphone button with recording state management\n- MediaRecorder captures audio chunks, sends as blob on stop\n- Visual feedback: listening indicator (pulsing red dot), transcript preview, task status\n- Wake word hint: \"🎙️ Say: Hey ACC, ...\"\n- Handles errors gracefully with user-friendly messages\n\n### Risks & Mitigations\n\n| Risk | Mitigation |\n|------|------------|\n| Audio file too large | Added 10MB file size limit via multer |\n| Whisper API failure | Fallback to DeepSeek transcription |\n| No audio permission | Graceful error handling with user prompt |\n| Browser compatibility | Uses standard Web APIs; Electron-compatible |\n| Security: audio data | No permanent storage; processed in memory |\n\n### Next Steps\n\n1. Add WebSocket support for real-time transcription streaming\n2. Implement voice activity detection (VAD) for automatic stop\n3. Add user authentication to voice route\n4. Create unit tests for wake word parsing\n5. Add support for multiple languages\n\n## Complete Files\n\n### File 1: cloud/connectors/voiceRoute.js\n\n```javascript\nconst express = require('express');\nconst multer = require('multer');\nconst axios = require('axios');\nconst FormData = require('form-data');\nconst fs = require('fs');\nconst path = require('path');\n\nconst router = express.Router();\n\n// Configure multer for audio file uploads (max 10MB)\nconst upload = multer({\n  storage: multer.memoryStorage(),\n  limits: {\n    fileSize: 10 * 1024 * 1024, // 10MB\n  },\n  fileFilter: (req, file, cb) => {\n    const allowedMimes = ['audio/webm', 'audio/wav', 'audio/mp3', 'audio/mpeg', 'audio/ogg', 'audio/mp4'];\n    if (allowedMimes.includes(file.mimetype)) {\n      cb(null, true);\n    } else {\n      cb(new Error('Invalid audio format. Supported: webm, wav, mp3, ogg, mp4'), false);\n    }\n  }\n});\n\n/**\n * POST /api/voice/transcribe\n * Accepts audio file (multipart/form-data, field: audio) OR text field\n * Returns transcribed text with wake word stripped and routed to Task Bus\n */\nrouter.post('/transcribe', upload.single('audio'), async (req, res) => {\n  try {\n    let transcript = '';\n    let transcriptionMethod = 'direct_text';\n\n    // Case 1: Text provided directly\n    if (req.body && req.body.text) {\n      transcript = req.body.text;\n      transcriptionMethod = 'direct_text';\n    }\n    // Case 2: Audio file provided\n    else if (req.file) {\n      const audioBuffer = req.file.buffer;\n      const mimeType = req.file.mimetype;\n      \n      // Try OpenAI Whisper first\n      if (process.env.OPENAI_API_KEY) {\n        try {\n          transcript = await transcribeWithWhisper(audioBuffer, mimeType);\n          transcriptionMethod = 'openai_whisper';\n        } catch (whisperError) {\n          console.warn('Whisper transcription failed, falling back to DeepSeek:', whisperError.message);\n          transcript = await transcribeWithDeepSeek(audioBuffer, mimeType);\n          transcriptionMethod = 'deepseek_fallback';\n        }\n      } else {\n        // No OpenAI key, use DeepSeek directly\n        transcript = await transcribeWithDeepSeek(audioBuffer, mimeType);\n        transcriptionMethod = 'deepseek';\n      }\n    } else {\n      return res.status(400).json({\n        success: false,\n        error: 'No audio file or text provided. Send audio (field: audio) or text (field: text).'\n      });\n    }\n\n    // Strip wake words\n    const cleanedTranscript = stripWakeWord(transcript);\n    \n    if (!cleanedTranscript || cleanedTranscript.trim().length === 0) {\n      return res.status(400).json({\n        success: false,\n        transcript: transcript,\n        error: 'Only wake word detected. Please provide a command after \"Hey ACC\".'\n      });\n    }\n\n    // Route to Task Bus\n    const taskBusUrl = `${req.protocol}://${req.get('host')}/api/taskbus/task`;\n    let taskResult;\n    try {\n      taskResult = await axios.post(taskBusUrl, {\n        command: cleanedTranscript.trim(),\n        source: 'voice',\n        metadata: {\n          transcriptionMethod,\n          originalTranscript: transcript,\n          timestamp: new Date().toISOString()\n        }\n      });\n    } catch (taskBusError) {\n      console.error('Task Bus routing failed:', taskBusError.message);\n      return res.status(502).json({\n        success: false,\n        transcript: cleanedTranscript.trim(),\n        error: 'Failed to route command to Task Bus. Is the service running?',\n        routing: { attempted: true, success: false }\n      });\n    }\n\n    return res.json({\n      success: true,\n      transcript: cleanedTranscript.trim(),\n      originalTranscript: transcript,\n      taskId: taskResult.data?.taskId || null,\n      routing: {\n        attempted: true,\n        success: true,\n        taskBusResponse: taskResult.data\n      },\n      transcriptionMethod\n    });\n\n  } catch (error) {\n    console.error('Voice route error:', error);\n    return res.status(500).json({\n      success: false,\n      error: 'Internal server error processing voice command.',\n      details: process.env.NODE_ENV === 'development' ? error.message : undefined\n    });\n  }\n});\n\n/**\n * Transcribe audio using OpenAI Whisper API\n */\nasync function transcribeWithWhisper(audioBuffer, mimeType) {\n  const formData = new FormData();\n  \n  // Determine file extension from mime type\n  const ext = mimeType.split('/')[1] || 'webm';\n  const fileName = `audio.${ext}`;\n  \n  formData.append('file', audioBuffer, {\n    filename: fileName,\n    contentType: mimeType,\n    knownLength: audioBuffer.length\n  });\n  formData.append('model', 'whisper-1');\n