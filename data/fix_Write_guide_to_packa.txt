```json
{
  "summary": "Complete guide for packaging ACC v2 Electron app as Windows installer using electron-builder.",
  "output": "# Guide: Packaging ACC v2 Electron App as Windows Installer\n\nThis guide walks through packaging the ACC v2 Electron desktop app as a Windows installer (.exe) using `electron-builder` with NSIS.\n\n## Prerequisites\n- Node.js 18+ installed\n- Electron app at `C:\\Users\\Shaya\\agent-command-center\\desktop\\`\n- React dashboard built at `ui/dist/`\n- Windows machine (for building Windows installer)\n\n---\n\n## Step 1: Install electron-builder\n\nOpen a terminal in the `desktop/` directory:\n\n```bash\ncd C:\\Users\\Shaya\\agent-command-center\\desktop\nnpm install --save-dev electron-builder\n```\n\n---\n\n## Step 2: Configure package.json for electron-builder\n\nEdit `desktop/package.json` to add the build configuration. Below is the exact configuration for a Windows NSIS installer:\n\n```json\n{\n  \"name\": \"acc-desktop\",\n  \"version\": \"1.0.0\",\n  \"description\": \"Agent Command Center v2 Desktop App\",\n  \"main\": \"main.js\",\n  \"scripts\": {\n    \"start\": \"electron .\",\n    \"build\": \"electron-builder build --win\",\n    \"build:dir\": \"electron-builder build --win --dir\"\n  },\n  \"build\": {\n    \"appId\": \"com.agent-command-center.app\",\n    \"productName\": \"ACC\",\n    \"directories\": {\n      \"output\": \"dist\"\n    },\n    \"files\": [\n      \"main.js\",\n      \"preload.js\",\n      \"ui/dist/**/*\",\n      \"node_modules/**/*\",\n      \"package.json\"\n    ],\n    \"win\": {\n      \"target\": [\n        {\n          \"target\": \"nsis\",\n          \"arch\": [\"x64\"]\n        }\n      ],\n      \"icon\": \"assets/icon.ico\"\n    },\n    \"nsis\": {\n      \"oneClick\": false,\n      \"perMachine\": false,\n      \"allowToChangeInstallationDirectory\": true,\n      \"createDesktopShortcut\": true,\n      \"createStartMenuShortcut\": true,\n      \"shortcutName\": \"ACC\",\n      \"installerIcon\": \"assets/icon.ico\",\n      \"uninstallerIcon\": \"assets/icon.ico\",\n      \"installerHeaderIcon\": \"assets/icon.ico\",\n      \"license\": \"LICENSE.txt\",\n      \"runAfterFinish\": true\n    }\n  },\n  \"devDependencies\": {\n    \"electron\": \"^28.0.0\",\n    \"electron-builder\": \"^24.0.0\"\n  },\n  \"dependencies\": {\n    \"electron-store\": \"^8.1.0\"\n  }\n}\n```\n\n**Key points:**\n- `files` includes only necessary files (main.js, preload.js, built UI, node_modules)\n- `win.target` set to NSIS for Windows installer\n- `icon` points to `assets/icon.ico` (must be .ico format, 256x256 recommended)\n- NSIS options: non-one-click installer, user can choose install directory, creates desktop/start menu shortcuts\n\n---\n\n## Step 3: Create Application Icon\n\nYou need a Windows icon file (.ico) at `desktop/assets/icon.ico`.\n\n### Option A: Convert existing image to .ico\n1. Use an online converter (e.g., https://convertio.co/png-ico/) or ImageMagick:\n   ```bash\n   # If ImageMagick is installed\n   magick convert icon.png -resize 256x256 icon.ico\n   ```\n2. Save as `desktop/assets/icon.ico`\n\n### Option B: Create from scratch\n1. Use a tool like GIMP or Paint.NET to create a 256x256 image\n2. Export as .ico with multiple sizes (16x16, 32x32, 48x48, 256x256)\n\n**Minimum requirement:** A 256x256 .ico file at `desktop/assets/icon.ico`.\n\n---\n\n## Step 4: Build the Installer\n\nRun the build command:\n\n```bash\ncd C:\\Users\\Shaya\\agent-command-center\\desktop\nnpx electron-builder build --win\n```\n\nThis will:\n- Compile the Electron app\n- Package it into an NSIS installer\n- Output to `desktop/dist/`\n\nExpected output file: `ACC Setup 1.0.0.exe` (version may vary)\n\n---\n\n## Step 5: Update .gitignore\n\nAdd to `desktop/.gitignore` to ignore build output:\n\n```\n# electron-builder output\ndist/\n*.exe\n*.msi\n*.blockmap\n```\n\n---\n\n## Step 6: Auto-Start on Windows Login\n\nTo make the app auto-start on Windows login, modify `main.js` to use Electron's `app.setLoginItemSettings()`:\n\n```javascript\nconst { app } = require('electron');\n\n// After app is ready\napp.on('ready', () => {\n  // Enable auto-start\n  app.setLoginItemSettings({\n    openAtLogin: true,\n    path: app.getPath('exe'),\n    args: ['--hidden']  // optional: start minimized\n  });\n});\n```\n\n**Alternative:** Let users enable/disable via settings UI:\n```javascript\n// In your settings/preferences handler\nconst { app } = require('electron');\n\nfunction setAutoStart(enabled) {\n  app.setLoginItemSettings({\n    openAtLogin: enabled,\n    path: app.getPath('exe')\n  });\n}\n```\n\n---\n\n## Step 7: Distribution\n\n1. **Locate the installer:** `desktop/dist/ACC Setup 1.0.0.exe`\n2. **Share the .exe file** directly with users (e.g., via email, cloud storage, or internal server)\n3. **Optional:** Sign the installer with a code signing certificate to avoid Windows SmartScreen warnings:\n   - Purchase a code signing certificate (e.g., from DigiCert, Sectigo)\n   - Configure in `package.json`:\n     ```json\n     \"win\": {\n       \"certificateFile\": \"path/to/cert.p12\",\n       \"certificatePassword\": \"your-password\"\n     }\n     ```\n   - Or use Azure Key Vault / hardware token\n\n**Important:** Users must have [WebView2 Runtime](https://developer.microsoft.com/en-us/microsoft-edge/webview2/) installed (if using Electron with WebView2). Electron bundles Chromium, so this is usually not needed.\n\n---\n\n## Troubleshooting\n\n| Issue | Solution |\n|-------|----------|\n| `icon.ico` not found | Ensure file exists at `desktop/assets/icon.ico` |\n| Build fails with missing dependencies | Run `npm install` first |\n| NSIS not found | Install NSIS (optional, electron-builder bundles it) |\n| Antivirus flags installer | Sign the installer with a code signing certificate |\n\n---\n\n## Complete File Structure (after build)\n\n```\ndesktop/\n├── assets/\n│   └── icon.ico\n├── ui/\n│   └── dist/\n│       ├── index.html\n│       └── ... (React build output)\n├── main.js\n├── preload.js\n├── package.json\n├── node_modules/\n├── dist/           (created by build)\n│   └── ACC Setup 1.0.0.exe\n└── .gitignore\n```\n\n---\n\n## Next Steps\n\n1. Test the installer on a clean Windows VM\n2. Consider adding auto-update (electron-updater) for future releases\n3. Add code signing for REDACTED distribution",
  "files_changed": [
    "desktop/package.json",
    "desktop/.gitignore",
    "desktop/main.js (optional for auto-start)"
  ],
  "risks": [
    "Missing icon.ico will cause build failure",
    "Unverified installer may trigger Windows SmartScreen",
    "Auto-start without user consent may be intrusive",
    "Large installer size if node_modules not pruned