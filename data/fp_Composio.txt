```json
{
  "summary": "Complete Composio integration setup with connection guide, action handlers, and Telegram command parsing for ACC v2.",
  "output": "## Composio Integration Setup for ACC v2\n\n### 1. Setup Script: `cloud/integrations/composio_setup.js`\n\n```javascript\n// cloud/integrations/composio_setup.js\n// One-time setup script to verify Composio connection and list available apps\n\nconst axios = require('axios');\nrequire('dotenv').config({ path: '../../.env' });\n\nconst COMPOSIO_API_KEY = process.env.COMPOSIO_API_KEY;\nconst COMPOSIO_BASE_URL = 'https://backend.composio.dev/api/v1';\n\nasync function setupComposio() {\n  console.log('🔧 Composio Setup - ACC v2');\n  console.log('===========================');\n\n  if (!COMPOSIO_API_KEY) {\n    console.error('❌ COMPOSIO_API_KEY not found in .env');\n    process.exit(1);\n  }\n\n  const headers = {\n    'x-api-key': COMPOSIO_API_KEY,\n    'Content-Type': 'application/json'\n  };\n\n  try {\n    // Step 1: Check connected accounts\n    console.log('\\n📡 Checking connected accounts...');\n    const connectedResponse = await axios.get(\n      `${COMPOSIO_BASE_URL}/connectedAccounts`,\n      { headers }\n    );\n\n    const accounts = connectedResponse.data?.items || [];\n    console.log(`✅ Found ${accounts.length} connected account(s):`);\n    accounts.forEach(acc => {\n      console.log(`   - ${acc.appName} (${acc.status})`);\n    });\n\n    // Step 2: List available apps\n    console.log('\\n📋 Available apps for integration:');\n    const availableApps = [\n      'Gmail', 'Slack', 'GitHub', 'Linear', 'Notion', 'HubSpot', 'Google Calendar'\n    ];\n    \n    for (const app of availableApps) {\n      try {\n        const appResponse = await axios.get(\n          `${COMPOSIO_BASE_URL}/apps/${app.toLowerCase()}`,\n          { headers }\n        );\n        console.log(`   ✅ ${app} - Available`);\n      } catch (err) {\n        if (err.response?.status === 404) {\n          console.log(`   ⚠️ ${app} - Not found (may need to enable)`);\n        } else {\n          console.log(`   ❌ ${app} - Error: ${err.message}`);\n        }\n      }\n    }\n\n    // Step 3: Return setup status\n    const setupStatus = {\n      status: 'completed',\n      connectedAccounts: accounts.length,\n      accounts: accounts.map(a => ({ app: a.appName, status: a.status })),\n      timestamp: new Date().toISOString()\n    };\n\n    console.log('\\n✅ Composio setup completed successfully');\n    return setupStatus;\n\n  } catch (error) {\n    console.error('❌ Setup failed:', error.message);\n    if (error.response) {\n      console.error('Response:', error.response.data);\n    }\n    process.exit(1);\n  }\n}\n\n// Run if executed directly\nif (require.main === module) {\n  setupComposio().then(status => {\n    console.log('\\n📊 Setup Status:', JSON.stringify(status, null, 2));\n  });\n}\n\nmodule.exports = { setupComposio };\n```\n\n### 2. Updated Action Handler: `cloud/integrations/composio.js`\n\n```javascript\n// cloud/integrations/composio.js\n// Updated with specific action handlers for ACC v2\n\nconst axios = require('axios');\nrequire('dotenv').config({ path: '../../.env' });\n\nconst COMPOSIO_API_KEY = process.env.COMPOSIO_API_KEY;\nconst COMPOSIO_BASE_URL = 'https://backend.composio.dev/api/v1';\n\nconst headers = {\n  'x-api-key': COMPOSIO_API_KEY,\n  'Content-Type': 'application/json'\n};\n\n/**\n * Execute a Composio action\n * @param {string} action - The action to execute (e.g., 'gmail_send_email')\n * @param {object} params - Parameters for the action\n * @returns {Promise<object>} - Result of the action\n */\nasync function executeAction(action, params = {}) {\n  console.log(`🚀 Executing Composio action: ${action}`);\n  \n  if (!COMPOSIO_API_KEY) {\n    throw new Error('COMPOSIO_API_KEY not configured');\n  }\n\n  // Map action names to Composio API endpoints\n  const actionMap = {\n    'gmail_send_email': {\n      endpoint: '/actions/gmail_send_email/execute',\n      requiredParams: ['to', 'subject', 'body']\n    },\n    'slack_send_message': {\n      endpoint: '/actions/slack_send_message/execute',\n      requiredParams: ['channel', 'text']\n    },\n    'github_create_issue': {\n      endpoint: '/actions/github_create_issue/execute',\n      requiredParams: ['repo', 'title', 'body']\n    },\n    'notion_create_page': {\n      endpoint: '/actions/notion_create_page/execute',\n      requiredParams: ['parent_id', 'title', 'content']\n    },\n    'calendar_create_event': {\n      endpoint: '/actions/calendar_create_event/execute',\n      requiredParams: ['summary', 'start_time', 'end_time']\n    }\n  };\n\n  const actionConfig = actionMap[action];\n  if (!actionConfig) {\n    throw new Error(`Unknown action: ${action}. Supported actions: ${Object.keys(actionMap).join(', ')}`);\n  }\n\n  // Validate required parameters\n  for (const param of actionConfig.requiredParams) {\n    if (!params[param]) {\n      throw new Error(`Missing required parameter: ${param} for action ${action}`);\n    }\n  }\n\n  try {\n    const response = await axios.post(\n      `${COMPOSIO_BASE_URL}${actionConfig.endpoint}`,\n      { params },\n      { headers }\n    );\n    \n    console.log(`✅ Action ${action} executed successfully`);\n    return response.data;\n  } catch (error) {\n    console.error(`❌ Failed to execute ${action}:`, error.message);\n    if (error.response) {\n      console.error('Response:', error.response.data);\n    }\n    throw error;\n  }\n}\n\n/**\n * Parse natural language command into action and parameters\n * @param {string} command - Natural language command\n * @returns {object} - { action, params }\n */\nfunction parseCommand(command) {\n  const lower = command.toLowerCase().trim();\n  \n  // Gmail: send email to [email] saying [message]\n  const emailMatch = lower.match(/send email to (\\S+) saying (.+)/i);\n  if (emailMatch) {\n    return {\n      action: 'gmail_send_email',\n      params: {\n        to: emailMatch[1],\n        subject: 'Message from ACC v2',\n        body: emailMatch[2]\n      }\n    };\n  }\n\n  // Slack: send slack message to [#channel] saying [message]\n  const slackMatch = lower.match(/send slack message to ([\\w-]+) saying (.+)/i);\n  if (slackMatch) {\n    return {\n      action: 'slack_send_message',\n      params: {\n        channel: slackMatch[1],\n        text: slackMatch[2]\n      }\n    };\n  }\n\n  // GitHub: create github issue titled [title]\n  const githubMatch = lower.match(/create github issue titled (.+)/i);\n  if (githubMatch) {\n    return {\n      action: 'github_create_issue',\n      params: {\n        repo: process.env.GITHUB_DEFAULT_REPO || 'default-repo',\n        title: githubMatch[1],\n        body: 'Created via ACC v2 Composio integration'\n      }\n    };\n  }\n\n  // Notion: create notion page titled [title]\n  const notionMatch