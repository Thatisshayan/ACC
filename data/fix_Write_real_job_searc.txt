```json
{
  "summary": "Created cloud/connectors/jobSearch.js with 4 fallback job search APIs (SerpAPI, Adzuna, RemoteOK, DeepSeek simulation) for ACC v2.",
  "output": "```javascript\n// cloud/connectors/jobSearch.js\n// Real job search connector with 4 fallback options for ACC v2\n// No browser/Playwright needed\n\nconst axios = require('axios');\n\n// ==================== Configuration ====================\nconst CONFIG = {\n  serpapi: {\n    key: process.env.SERPAPI_KEY || '',\n    baseUrl: 'https://serpapi.com/search',\n  },\n  adzuna: {\n    appId: process.env.ADZUNA_APP_ID || '',\n    appKey: process.env.ADZUNA_APP_KEY || '',\n    baseUrl: 'https://api.adzuna.com/v1/api/jobs',\n  },\n  remoteok: {\n    baseUrl: 'https://remoteok.com/api',\n  },\n};\n\n// ==================== Helper Functions ====================\n\n/**\n * Check if any job search source is available\n * Always returns true because DeepSeek simulation is fallback\n */\nfunction enabled() {\n  return true;\n}\n\n/**\n * Format salary range into readable string\n */\nfunction formatSalary(min, max, currency = 'USD') {\n  if (!min && !max) return 'Salary not specified';\n  if (min && max) return `${currency} ${min.toLocaleString()} - ${max.toLocaleString()}`;\n  if (min) return `From ${currency} ${min.toLocaleString()}`;\n  return `Up to ${currency} ${max.toLocaleString()}`;\n}\n\n/**\n * Clean HTML tags from description\n */\nfunction cleanHtml(html) {\n  if (!html) return '';\n  return html.replace(/<[^>]*>/g, '').replace(/&amp;/g, '&').replace(/&lt;/g, '<').replace(/&gt;/g, '>').replace(/&quot;/g, '\"').replace(/&#39;/g, \"'\");\n}\n\n// ==================== API Connectors ====================\n\n/**\n * Option 1: SerpAPI Google Jobs\n * Free tier: 50 searches/month\n */\nasync function searchSerpapi(role, location, jobType, limit) {\n  if (!CONFIG.serpapi.key) {\n    throw new Error('SERPAPI_KEY not configured');\n  }\n\n  const params = {\n    engine: 'google_jobs',\n    q: `${role} jobs in ${location}`,\n    api_key: CONFIG.serpapi.key,\n    hl: 'en',\n    gl: 'us',\n  };\n\n  if (jobType && jobType !== 'any') {\n    params.q += ` ${jobType}`;\n  }\n\n  const response = await axios.get(CONFIG.serpapi.baseUrl, { params, timeout: 10000 });\n  const jobs = response.data.jobs_results || [];\n\n  return jobs.slice(0, limit || 10).map(job => ({\n    title: job.title || 'Unknown Position',\n    company: job.company_name || 'Unknown Company',\n    location: job.location || location,\n    salary: job.salary || 'Salary not specified',\n    url: job.related_links?.[0]?.link || job.share_link || '',\n    description: cleanHtml(job.description || ''),\n    source: 'SerpAPI',\n    posted: job.detected_extensions?.posted_at || '',\n    schedule: job.detected_extensions?.schedule_type || '',\n  }));\n}\n\n/**\n * Option 2: Adzuna API\n * Free tier available\n */\nasync function searchAdzuna(role, location, jobType, limit) {\n  if (!CONFIG.adzuna.appId || !CONFIG.adzuna.appKey) {\n    throw new Error('ADZUNA_APP_ID or ADZUNA_APP_KEY not configured');\n  }\n\n  // Default to Canada (ca) - can be made configurable\n  const country = 'ca';\n  const params = {\n    app_id: CONFIG.adzuna.appId,\n    app_key: CONFIG.adzuna.appKey,\n    what: role,\n    where: location,\n    results_per_page: limit || 10,\n    content_type: 'application/json',\n  };\n\n  if (jobType && jobType !== 'any') {\n    params.what += ` ${jobType}`;\n  }\n\n  const response = await axios.get(\n    `${CONFIG.adzuna.baseUrl}/${country}/search/1`,\n    { params, timeout: 10000 }\n  );\n\n  const jobs = response.data.results || [];\n\n  return jobs.map(job => ({\n    title: job.title || 'Unknown Position',\n    company: job.company?.display_name || 'Unknown Company',\n    location: job.location?.display_name || location,\n    salary: formatSalary(job.salary_min, job.salary_max, job.salary_currency || 'USD'),\n    url: job.redirect_url || '',\n    description: cleanHtml(job.description || ''),\n    source: 'Adzuna',\n    posted: job.created || '',\n    schedule: job.contract_type || '',\n  }));\n}\n\n/**\n * Option 3: RemoteOK API\n * Completely free, no key needed\n */\nasync function searchRemoteok(role, location, jobType, limit) {\n  const params = {};\n  if (role) {\n    params.tag = role.toLowerCase().replace(/\\s+/g, '-');\n  }\n\n  const response = await axios.get(CONFIG.remoteok.baseUrl, {\n    params,\n    timeout: 10000,\n    headers: {\n      'Accept': 'application/json',\n    },\n  });\n\n  let jobs = response.data;\n  if (!Array.isArray(jobs)) {\n    jobs = [];\n  }\n\n  // Filter by location if specified (RemoteOK is remote, but we can filter)\n  if (location && location.toLowerCase() !== 'remote') {\n    jobs = jobs.filter(job => {\n      const loc = (job.location || '').toLowerCase();\n      return loc.includes(location.toLowerCase());\n    });\n  }\n\n  // Filter by job type\n  if (jobType && jobType !== 'any') {\n    jobs = jobs.filter(job => {\n      const tags = (job.tags || []).map(t => t.toLowerCase());\n      return tags.includes(jobType.toLowerCase());\n    });\n  }\n\n  return jobs.slice(0, limit || 10).map(job => ({\n    title: job.position || 'Unknown Position',\n    company: job.company || 'Unknown Company',\n    location: job.location || 'Remote',\n    salary: job.salary || 'Salary not specified',\n    url: job.url || `https://remoteok.com/remote-jobs/${job.slug}`,\n    description: cleanHtml(job.description || ''),\n    source: 'RemoteOK',\n    posted: job.date || '',\n    schedule: 'Remote',\n  }));\n}\n\n/**\n * Option 4: DeepSeek Simulation\n * Generates realistic job listings when APIs fail\n */\nasync function searchDeepSeek(role, location, jobType, limit) {\n  const count = limit || 5;\n  const jobs = [];\n  \n  const companies = [\n    'TechCorp', 'DataFlow Inc.', 'CloudBase Systems', 'InnovateTech',\n    'NextGen Solutions', 'Alpha Digital', 'Prime Software', 'Vertex AI',\n    'Synergy Labs', 'Pioneer Tech', 'Quantum Dynamics', 'Apex Systems'\n  ];\n  \n  const locations = location ? [location] : ['Remote', 'San Francisco, CA', 'New York, NY', 'Austin, TX', 'Seattle, WA'];\n  \n  const salaryRanges = [\n    { min: 80000, max: 120000 },\n    { min: 100000, max: 150000 },\n    { min: 120000, max: 180000 },\n    { min: 90000, max: 130000 },\