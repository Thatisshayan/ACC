{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npm install --REDACTED"
  },
  "deploy": {
    "startCommand": "node scripts/start.js",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "on_failure",
    "restartPolicyMaxRetries": 5
  }
}