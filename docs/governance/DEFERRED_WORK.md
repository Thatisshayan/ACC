# Deferred Work Register

Rule 12 / Rule 11. This register survives the session. Future agents resume from here.

## Format
- `[DATE] <scope>: <what> — <why deferred> — <resume hint> — <status>`

## Items
- `[2026-08-21] cloud/taskbus/router.js: bypass agents (`imagegen`, `image`, `tavily`, `hunter`, `alibaba`, `qwen`) skipped the approval gate even when their specialized connector was unavailable and execution fell through to the generic provider chain. Fixed in this session by re-entering the standard safety path on fallback and adding regression coverage in `cloud/taskbus/router.test.js`. — resolved 2026-08-21`
- `[2026-08-21] cloud/graphRunner.service.js: snapshot approval was not actually gating graph execution; `_handleSnapshot()` marked a node `pendingApproval`, but `_executeNode()` immediately overwrote it to `completed` and unlocked dependents. Fixed in this session by making `pendingApproval` a real wait state reconciled by the run loop and covered by `cloud/graphRunner.service.test.js`. — resolved 2026-08-21`
- `[2026-08-21] cloud/security/ephemeralSnapshots.js: snapshot files were written to disk but never reloaded into memory on startup, so pending approvals were lost across restart despite persisted files remaining on disk. Fixed in this session by hydrating persisted snapshots at boot and rescheduling TTL cleanup. — resolved 2026-08-21`
- `[2026-08-21] external-action control plane: direct side-effect routes bypassed the central approval-aware task model, notably `cloud/api/phoneRoutes.js` (`/sms`, `/call`) and `cloud/taskbus/routes.js` (`/socialclaw/publish`, `/socialclaw/delete`). Fixed in this session by routing both families into approval-gated taskbus tasks and adding regression coverage in `cloud/api/phoneRoutes.test.js`, `cloud/taskbus/routes.test.js`, and `cloud/taskbus/router.test.js`. — resolved 2026-08-21`
- `[2026-07-29] repo-root-parity: 26 git-tracked legacy files from the previous root (C:\Users\Shaya\agent-command-center) are not present in the current root (D:\AgentDevWork\repos\ACC), all inside five AGENT_* handoff/report folders — deferred because this session was scoped to audit/reporting rather than migration; if parity is required, recover or intentionally archive those folders and update docs to mark the decision — open`
- `[2026-08-01] cloud/queue.js: enqueueTask has no input validation — a missing agentType/payload silently creates a queued task that the worker will run with undefined fields. Found while writing cloud/queue.test.js (Task E); the handoff scoped that file to the modules that exist, not new behavior. Resume hint: add early-return validation in enqueueTask and test it. — open`
- `[2026-08-01] scripts/verify.ps1 (Windows): npm ci stage runs close to the 300s RunTimed budget on slow local machines (~7 min here) and can be killed mid-install, cascading into a failed deploy-dry (node_modules incomplete). The install itself succeeds and CI runners stay within budget. Resume hint: if local verify runs matter on Windows, make the npm-ci timeout configurable (e.g. $env:VERIFY_NPM_CI_TIMEOUT) without loosening the CI default. — open`
- `[2026-08-01] secret-scan: local runtime artifact data/messages/messenger.key (gitignored via data/messages/, untracked) is flagged by the file-based .key scan. Clean CI checkouts are unaffected. Resume hint: consider excluding data/ runtime dirs from the filename scan, or documenting that the file must be deleted after local runs. — open`
- `[2026-08-01] cloud/server.js: GET /api/task/:id was unauthenticated — leaked task status/result/error/meta for any known task UUID. Fixed across sessions by adding `requireOperatorOrAdmin` to the route and updating README so it is no longer documented as public. Residual follow-up: if the legacy queue API is retired, remove the route and dead `pollOldTask` references with explicit approval. — resolved 2026-08-21`
- `[2026-08-01] deps: node-telegram-bot-api is an unused direct dependency — a repo-wide grep shows ZERO import sites (cloud/telegram/bot.js is a custom raw-HTTP Telegram client, not this library). Upgraded ^0.67.0 → ^1.2.0 in Phase 4 to clear the audit tree, but it could be removed entirely. Resume hint: `npm uninstall node-telegram-bot-api` + update CLAUDE.MD reference (R14: needs approval before removing a dependency). — open`
- `[2026-08-01] dead-code: cloud/roles/approvalQueue.js — orphan module, zero incoming require() references repo-wide (Phase 5, scripts/findOrphanModules.js). In-memory approval queue; superseded-by unclear (approval flow now lives in taskbus/hub). Needs human review. R14: kept, no deletion. — open`
- `[2026-08-01] dead-code: cloud/roles/negotiationPolicy.js — orphan module, zero incoming require() references repo-wide. Duplicate of cloud/utils/negotiationPolicy.js. Needs human review. R14: kept, no deletion. — open`
- `[2026-08-01] dead-code: cloud/roles/roleDefinitions.js — orphan module, zero incoming require() references repo-wide. Canonical role definitions; not wired to any auth/middleware. Needs human review. R14: kept, no deletion. — open`
- `[2026-08-01] dead-code: cloud/api.js — orphan module (zero incoming requires). Legacy express router that starts its own worker via startWorker; superseded by cloud/server.js + cloud/api/*Routes.js mounts. Needs human review. R14: kept, no deletion. — open`
- `[2026-08-01] dead-code: cloud/router.js — orphan module (zero incoming requires). Legacy role→provider mapping (`role === "architect"` → "copilot" etc.); superseded by cloud/taskbus/router.js + providerFallback. Needs human review. R14: kept, no deletion. — open`
- `[2026-08-01] dead-code: cloud/orchestrator/graphRunner.js — orphan module (zero incoming requires). Legacy graph-step runner enqueuing via queue.js; superseded by graphRunner.service.js + taskbus execution path. Needs human review. R14: kept, no deletion. — open`
- `[2026-08-01] dead-code: cloud/taskbus/localEngine.js — orphan module (zero incoming requires). "Free local LLM" stub execution path; superseded by providerFallback (deepseek→perplexity→alibaba→ollama). Needs human review. R14: kept, no deletion. — open`
- `[2026-08-01] dead-code: cloud/security/guardrails.js + cloud/security/tokenManager.js — orphan modules (zero incoming requires). Guardrail checks / token issuance; superseded by cloud/middleware/auth.js + webhookHmac. Needs human review. R14: kept, no deletion. — open`
- `[2026-08-01] dead-code: cloud/utils/helpers.js + cloud/utils/campaignManager.js + cloud/utils/negotiationPolicy.js — orphan modules (zero incoming requires). helpers is generic JSON/time utils (likely superseded by in-file helpers); campaignManager is opt-in/unsubscribe quota logic not wired to any campaign; negotiationPolicy duplicates roles/negotiationPolicy.js. Needs human review. R14: kept, no deletion. — open`
- `[2026-08-01] dead-code: cloud/limits/roleLimits.js — orphan module (zero incoming requires). Role rate-limit constants; not referenced by auth/middleware or taskbus. Needs human review. R14: kept, no deletion. — open`
- `[2026-08-01] dead-code: cloud/graphs/graphBuilderV2.js + cloud/telegram/graphBuilderV2.js — orphan modules (zero incoming requires). graphBuilderV2 canonical version lives in cloud/graphs/; telegram/graphBuilderV2.js is a duplicate/re-export. Both unreferenced (bot.js doesn't import them). Needs human review. R14: kept, no deletion. — open`
- `[2026-08-01] dead-code: cloud/telegram/graphBuilder.js + cloud/telegram/buttons.js + cloud/telegram/handlers.js + cloud/telegram/features/imageAnalysis.js — orphan modules (zero incoming requires). Legacy Telegram graph builder v1 (superseded by graphBuilderV2), menu-button definitions (superseded by inline keyboards in bot.js), and message/callback handlers (superseded by bot.js inline logic); imageAnalysis is a Feature-2 stub not referenced by bot.js. Needs human review. R14: kept, no deletion. — open`
- `[2026-08-01] dead-code: cloud/connectors/jobApplier.js + cloud/connectors/voiceRoute.js + cloud/connectors/whisper.js — orphan modules (zero incoming requires). Legacy Playwright job applier, voice transcription route, and Whisper STT wrapper — none registered in manifest.json or required by cloud/connectors/registry.js (which only loads manifest-listed adapters). Superseded by cloud/integrations/* or unused. Needs human review. R14: kept, no deletion. — open`
- `[2026-08-01] dead-code: cloud/connectors/integrations/*.js (clickup, fiverr, gcalendar, gdrive, gmail, googlejobs, indeed, linkedin, notion, shopify, stripe, upwork) — 12 orphan stub modules (zero incoming requires). Stub connectors printing "[x] Stub called" — NOT in manifest.json (the real adapters live in cloud/connectors/ + cloud/integrations/). Needs human review. R14: kept, no deletion. — open`
- `[2026-08-01] dead-code: cloud/workflows/crewai/job-workflow-runner.js — orphan module AND malformed file: content starts with `on { "summary": ... }` (a JSON/graph output was written into a .js file), fails to parse as JS (SyntaxError: Unexpected token ':'). Git-tracked. Needs human review to decide if it should be replaced with real code or the artifact removed (R14: needs approval before deletion). — open`
- `[2026-09-03] cloud/connectors/codeExec.js` + `cloud/messages/service.js`: `code.run`/`agent.http` chat intents were a critical RCE (unsandboxed `node:vm` escape) + SSRF (unrestricted outbound fetch, including cloud metadata endpoints) pair, reachable by any operator-role key via `POST /api/assistant/execute`. Fixed this session: `CODEEXEC_ENABLED` kill switch (default off), admin-only role gate with audit logging, `code.run` now evaluated by a dependency-free arithmetic parser (`cloud/utils/safeExpr.js`) instead of `vm`, SSRF guard blocking private/loopback/link-local/metadata addresses for both literal IPs and DNS-resolved hostnames, anchored intent matching. Full writeup: `docs/SECURITY_CODEEXEC.md`. Regression tests: `cloud/utils/safeExpr.test.js`, `cloud/connectors/codeExec.test.js`, `cloud/messages/service.test.js`. Full audit + everything else found/still open: `audits/2026-09-03_Claude_ComprehensiveAudit_and_LaunchReadiness.md`. — resolved 2026-09-03`
- `[2026-09-03] migrations/003_waitlist_columns.sql` alters `acc_waitlist` before `migrations/009_waitlist_base.sql` creates it; `scripts/migrate.js` applies files in order and throws on first failure, so a fresh Supabase project cannot bootstrap past migration 003. Root cause of the long-standing "run SQL tables on new Supabase project" pending item. Resume hint: renumber 009 ahead of 003, or fold table creation into 003. Not fixed this session (scoped to codeExec RCE/SSRF). — open`
- `[2026-09-03] cloud/services/emailMonitor.js:32` "encrypts" IMAP app passwords with `Buffer.from(password).toString('base64')` — trivially reversible, despite the migration column comment promising AES-256. A working AES-256-GCM helper already exists and is unused here: `cloud/messages/encryption.js`. Resume hint: route through that helper before insert. Not fixed this session. — open`
- `[2026-09-03] CLAUDE.md line ~82 ("Switched to nixpacks, deleted Dockerfile") is stale — `railway.json` currently builds from the root `Dockerfile` (present, real), and the two leftover `nixpacks.toml` files (root + `cloud/`) disagree with each other. Resume hint: delete both nixpacks.toml files unless intentionally switching back, in which case reconcile them first. Corrected the doc claim this session; did not touch the actual build config. — open`
- `[2026-09-03] .github/workflows/claude-code-review.yml: every open PR (12, 16, 18, 20, 21) has a failing `claude-review` check because the `ANTHROPIC_API_KEY` repo secret is unset — confirmed via job logs. Not a code issue in any of those PRs. `master` has no branch protection, so this has never actually blocked a merge, just left every PR showing a meaningless red X. Resume hint: add the secret in repo settings, or stop treating this check as signal. — open`
- `[2026-09-03] 46-alert CodeQL backlog on master (pre-existing, discovered while chasing PR #22's own CodeQL check): a dedicated same-day pass fixed the majority — most seriously, `cloud/hub/events.js`'s `EVENT_HANDLERS[event.type]` lookup let `event.type: 'constructor'` resolve through the inherited Object.prototype chain to the real Object constructor, letting a caller smuggle `approval_required: false` past the approval gate entirely (fixed: static switch dispatch, no lookup table). Also fixed: real SSRF in `cloud/hub/registry.js`/`commands.js` (webhookUrl) and `cloud/workflows/accOutreachCrmModule.js` (sheetCsvUrl) via a new shared `cloud/utils/ssrfGuard.js`; a path-traversal gap in `cloud/security/ephemeralSnapshots.js` that was only safe by an incidental Map-lookup elsewhere, not an explicit check; reflected XSS in `cloud/api/outreachRoutes.js` unsubscribe route; TwiML injection in `cloud/api/phoneRoutes.js` (already flagged separately in the audit doc); a broken SQL LIKE escape in `cloud/memory/store.js` (no ESCAPE clause, so the existing `%`-escaping was a no-op); a Telegram MarkdownV2 injection in `cloud/services/emailMonitor.js`'s `escTg()` (backslash not escaped first); a same-origin-check bug in `desktop/main.js` (`startsWith` instead of parsed-origin comparison); an actually-broken IMAP password "encryption" (base64, now real AES-256-GCM); a ReDoS-prone email regex in `cloud/server.js`; input-length capping in `cloud/messages/service.js` against 12 ReDoS-shaped intent-parsing regexes; a redundant Unicode range in `cloud/telegram/bot.js`; least-privilege `permissions:` on `.github/workflows/ci.yml`; and a 7-day un-`unref()`'d timer in `ephemeralSnapshots.js` that would hang a clean process shutdown (found while writing its regression test). A handful were confirmed false positives and left as-is (documented in `audits/2026-09-03_Claude_ComprehensiveAudit_and_LaunchReadiness.md` §0.1). Full detail there. — resolved 2026-09-03`
- `[2026-08-01] scripts/verify.sh (Git-Bash on Windows only): GNU coreutils `timeout` cannot spawn the Windows `node.exe` (`timeout: failed to execute process: No such file or directory (os error 2)`, rc=127), so the deploy-dry step reports failure and the orphan-lint guard (`command -v node`) skips — both purely local MSYS interop issues. On Linux CI (the actual gate) GNU `timeout node ...` works and `node` is on PATH, so deploy-dry + orphan-lint run correctly. Verified: `node scripts/deployDryRun.js` and `node scripts/findOrphanModules.js` both pass when invoked directly (not via GNU `timeout`). Resume hint: if local Git-Bash verify runs matter, wrap node in the full Windows path or use `msys2 sh`; otherwise leave as-is — CI is green. — open`
- `[2026-09-03] docs/governance/HANDOFF_2026-09-03_opencode-followup.md created — the next tier of safe-to-fix, no-secrets-needed work re-verified against current code after PR #22/#24 merged: migrations/003 vs 009 ordering (exact fix specified: fold a guarded CREATE TABLE into 003, don't renumber files), mobile app has no auth-credential concept at all in its session model (not just a missing header — `mobile/src/lib/session.tsx` only stores `currentUserId`/`apiBaseUrl`), CI (`ci.yml`) never runs `npm test`, `gate.yml`'s PR trigger targets a nonexistent `main` branch, `cloud/taskbus/persistence.js`'s `is_real_ai` vs `is_real_ai_result` field mismatch, and `cloud/api/phoneRoutes.js`'s Twilio webhook signature check failing because no `express.urlencoded()` middleware exists anywhere in the app. Resume hint: read the handoff doc, it's ready to execute as-is. — open`
- `[2026-09-03] cloud/orchestrator/graphRunner.js confirmed dead code via repo-wide grep today (nothing under cloud/ requires it) — the "uncapped retry loop with no DLQ wiring" finding from earlier in the day's audit doc (§5 item 2) is real but irrelevant in practice since nothing executes this file; `cloud/graphRunner.service.js` is the live runner. Resume hint: ask Shayan whether to delete (R14 applies) or repurpose; do not spend effort "fixing" its retry logic until that's decided. — open`
- `[2026-09-03] REPO_RULES.md and docs/governance/BRANCH_POLICY.md refer to the protected branch as `main` throughout (R26, R27, R30, and BRANCH_POLICY.md's whole "Branch strategy" section) — that branch does not exist in this repo; the real default branch is `master` (same root confusion as the already-documented `.github/workflows/gate.yml` bug). Not fixed this session — a repo-wide find/replace across governance docs felt like it needed a dedicated pass, not a drive-by edit while doing something else. Resume hint: grep both files for `main`, replace with `master`, and confirm no other governance doc has the same issue. — open`
- `[2026-09-03] cloud/utils/auditLog.js's in-memory-only audit trail (resets every restart) vs the unused `acc_audit_log` Supabase table (§5 item 3 in the audit doc) is real and worth fixing, but scoped out of the opencode handoff above as its own follow-up — it depends on the migration-ordering fix (Task A in the handoff) actually being verified against a live Supabase project first, which needs Shayan's access. Resume hint: once Task A lands and a real DB has been migrated successfully, this becomes a self-contained "wire logNodeRun() to also INSERT into acc_audit_log" task. — open`
